---
"@cloudflare/polystella-emdash": patch
---

Polish the EmDash plugin settings page: shrink the catalog selection column, debounce and extend the catalog search to source text, auto-sync the runtime override switch with enabled overrides, drop redundant headings and labels, and duplicate the catalog action row at the bottom of the list.
