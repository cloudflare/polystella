---
"@cloudflare/polystella-emdash": minor
---

Add the native EmDash plugin with deployment policy, saved-entry content translation, project collection settings, Workers AI binding routes, temporary catalog overrides, deterministic JSON export, and React admin UI.
