---
"@cloudflare/polystella-emdash": minor
"@cloudflare/polystella-cli": minor
"@cloudflare/polystella-astro": patch
---

Add the native EmDash plugin with administrator-owned collection policies, saved-entry content translation, per-locale model and glossary settings, shared instruction controls, request-scoped administrator debug traces, Workers AI binding or runtime-secret HTTP routes, temporary catalog overrides, deterministic JSON export, a Kumo-based React admin UI, companion Astro runtime catalog integration, and the existing catalog CLI commands. Extract those commands into a shared package so Astro and EmDash use the same implementation.
