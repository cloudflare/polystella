---
"@cloudflare/polystella-emdash": minor
---

Add a freeform translation sandbox with per-request allowlisted model selection, keep the source locale deployment-owned and out of target settings, and show named percentage stages during admin translation workflows. The catalog view gains group-title display from `i18n_group_title`, a search box that filters groups by title, group key, or entry keys, and tooltip guidance that overrides should retain `{{tokens}}` placeholders.
