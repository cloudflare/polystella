/** Relative to project root. */
export const DEFAULT_CATALOG_BASE = "./src/content/i18n";

/** JSON glob pattern so subdirectory layouts (`nav/en-US.json` etc.) work. */
export const DEFAULT_CATALOG_PATTERN = "**/*.json";
